# CatVTON-FLUX LoRA Fine-tuning Dependencies
# Install: pip install -r scripts/training/requirements.txt

# Core ML framework
torch>=2.3.0
torchvision>=0.18.0
accelerate>=0.30.0

# Diffusers for CatVTON pipeline
diffusers>=0.27.0
transformers>=4.36.0
safetensors>=0.4.0

# Image processing
Pillow>=10.0.0
numpy>=1.24.0
opencv-python>=4.8.0

# Training utilities
peft>=0.10.0        # LoRA training support
bitsandbytes>=0.43.0  # 8-bit Adam optimizer for memory savings
tensorboard>=2.16.0   # Training monitoring

# Dataset loading
datasets>=2.18.0

# Progress & logging
tqdm>=4.66.0
wandb>=0.16.0       # Optional: Weights & Biases logging

# For mask generation during dataset prep
rembg>=0.1.0
