# Kanchuki — Build Log (part 6 of 6)

> Continuation of [`../BUILD-LOG.md`](../BUILD-LOG.md) — split 2026-09-26 to stay under the 150k-char doc limit. Full chronological detail, same as before, just split by size. Sections in this part run from "2026-09-24 (launch §7A.7) — pre-production re-test of every RC-###: 40/40 pass" to "2026-09-25 (later still) — the mobile manifest stops repeating the app identity: the shared constant is now what `expo prebuild` reads".

---

## 2026-09-24 (launch §7A.7) — pre-production re-test of every RC-###: 40/40 pass

**Commit:** *(this task)* · docs only — no source change. No migration, no `apps/mobile` file, no build needed.

| Piece | Detail |
|---|---|
| Scope, made a closed set before testing | The board cited `docs/root-cause/README.md` — **that file does not exist** (the directory contains exactly one file, `root-cause issues.md`). It also asked for "every RC-###" without saying how many there are; the tracker holds **exactly 40 entries, RC-001…RC-040, contiguous** (`grep -cE '^## RC-[0-9]+'` → 40). A re-test whose scope is a guess is not a re-test, so the count was fixed first. |
| Suites re-run fresh | Run per package directly (`pnpm exec vitest run`), not via `pnpm test --force` (the flag leaks into vitest) and not via nested `turbo` (which pulls in dependency output): `apps/api` **1377 passed \| 5 skipped (1382)** across 98+1 files · `apps/web` **331/331** (43 files) · `apps/mobile` **107/107** (18 files) · `packages/shared` **36** · `packages/db` **25 passed \| 4 skipped (29)** · `packages/ai` **91**. **Zero failures anywhere.** |
| The 9 skips, accounted for | 5 are `apps/api/src/jobs/purge-rls-live.test.ts` (needs a real local Postgres; board **§2.1**) — its statically-checkable sibling `purge-rls-policy.test.ts` runs and covers the same rule, so RC-030 is still pinned. The other 4 are `packages/db`'s live-DB arms. An opt-in suite that skips loudly is the opposite of the RC-025/RC-038 failure shape; neither is a launch blocker. |
| Per-RC table | 40 rows written into the board (§7A.7), each naming the **executable arm that fails if the bug returns** — the test that spells out the RC where one exists, otherwise the behaviour test that guards the fix. All 40 tracker entries were also read entry by entry to confirm each carries its own recorded `- **Proof` line, so no fix in that file is unsupported. |
| Pins, by kind | 20 of the 40 RCs have their ID inside a test file (`grep -rlE 'RC-0[0-9][0-9]'` over `apps/` + `packages/` → 23 files); the other 20 are pinned by behaviour tests that do not spell the ID out — e.g. **RC-004** by `categories.test.ts` *"DELETE … purge-role guardrail"*, **RC-005** by `ProductDetailSheet.test.tsx` *"clicking a related product swaps the sheet to it"*, **RC-006** by `ShowcaseDesigns.test.tsx` (asserts `/meera-sarees/designs/d1`), **RC-040** by `store-seo.test.ts` (asserts `</script>` is absent from the output). The pin is the guard, not the comment. |
| Two RCs honestly have **no automated arm** | **RC-020** (a `transparent` OTP input force-rendered by some OEM keyboards) and **RC-021** (a screen dispatching two OTP sends — backend *and* widget) are React Native render-branch behaviours with no component-level surface in `apps/mobile` and no jsdom reproduction. Their API halves are pinned (`auth-msg91` 12/12, `auth-review-bypass` 10/10) but the mobile halves are **not**, so they are recorded as ⚠️ **→ §7B.8 real-device pass** rather than marked pass. Marking them green because the source looks fixed is exactly the RC-025 mistake (described in one place, verified in none). |
| Verdict | **40/40 pass**, 2 with the caveat above. Nothing in the tracker is unreproduced, nothing was found broken, and the remaining open items are owner actions (§7B) or the deferred §7A.5 load test. |

## 2026-09-24 (launch §7A.5) — k6 load-test scripts against staging + guard + runbook

**Commit:** *(this task)* · `scripts/load/k6/{mix,storefront,retailer}.js` + `apps/api/src/routes/load-test.test.ts` + `docs/SCALING.md (Load Testing section)`. No migration, no `apps/mobile` file, no `apps/web` file.

| Piece | Detail |
|---|---|
| What §7A.5 asked for | `docs/SCALING.md` §5: simulate a realistic mix — retailer photo uploads (write-heavy), collection-link views (read-heavy, public/anonymous), AI search queries — against a staging Supabase branch, never prod, with k6 or Artillery. |
| `scripts/load/k6/mix.js` | The scenario definition, deliberately **pure** — no `k6/*` imports, no `__ENV`, no I/O — so the same module that the two k6 entrypoints run is also importable and unit-tested under Node/vitest. Holds `assertSafeBaseUrl` (prod refusal, no override), `parseRate` (180/min self-cap, throws above it), `requireToken` (JWT shape check), `chooseEndpoint`/`totalWeight` (deterministic weighted selection, stable on negative/wrapping indexes), `buildPublicMix`, `buildRetailerMix`. |
| `scripts/load/k6/storefront.js` | Anonymous, read-heavy k6 entrypoint: 28% storefront · 20% product grid · 15% product detail · 12% categories · 10% related · 8% directory · 7% collection-`view` write. `setup()` resolves real store slugs / product ids / collection slugs from the target staging API and **fails** (not silently 404s) if staging has no live storefront or no products. |
| `scripts/load/k6/retailer.js` | Authenticated k6 entrypoint: 35% product list · 25% `GET /retailers/me` · 20% `GET /categories` · 20% `POST /products/upload-url` presign. Requires `LOADTEST_BEARER`; `setup()` proves the token against `GET /retailers/me` before spending the run on it. Never follows the presign with a PUT — nothing is ever written to R2, no product is ever created. |
| `apps/api/src/routes/load-test.test.ts` | The guard, **54/54 passing**. Derives the real route table from the API sources (`derivedRoutes()` — regex-scans `server.get/post/…` calls under the four mounts the mixes touch) so a renamed route fails CI instead of the owner's first run; asserts every mix entry resolves to a real route, that built paths still match their template (no `/products/undefined`), that the rate constants are pinned to `apps/api/src/index.ts`'s actual `@fastify/rate-limit` config, that the prod deny-list is pinned to the hosts `docs/DEPLOY.md` names, that `POST /v1/products` and every provider-spend/destructive route are banned by exact shape, and that a missing collection drops the `collection_view` entry instead of emitting `/null/view`. |
| Findings that shaped the design | (1) The API's global rate limiter (`max: 200/min` per IP, `apps/api/src/index.ts:132`, no env override, public routes not exempt) is the real ceiling — one generator is one IP sharing one budget across every endpoint; the script self-caps at 180/min and treats a 429 as `rate_limited` rather than `http_req_failed`, so a limiter-bound run can't be misread as a broken API. (2) The API never sees photo bytes — `POST /products/upload-url` presigns, the phone PUTs to R2 — so "write-heavy" on the API side is the presign, at zero external cost. (3) `POST /v1/products` buys a Vision call per request (`addTaggingJob()` unconditional in `products-crud.ts`) — banned by exact route shape. (4) `POST /v1/public/search` embeds every query with no cache — the one read path that bills per request, opt-in only via `LOADTEST_SEARCH=1`. (5) Production (`kanchuki.app`/`kanchuki.com`, from `docs/DEPLOY.md`) is refused with **no override flag** — an escape hatch on a load test is one typo from a traffic spike on live retailers. (6) k6 chosen over Artillery — no new workspace dependency, real arrival-rate model, named first in §5. |
| Guard falsified 4 ways, all restored | Renamed `/stores` → `/store-list` in `public-stores.ts` → the "is a registered route" arm went red (2 failures, missing-route message). Raised `SAFE_RATE_PER_MINUTE` to 300 in `mix.js` → both rate-rejection arms went red (200/min no longer exceeded the raised cap). Added a `POST /v1/products` entry to `buildRetailerMix()` → the create-route-ban arm went red, naming the Vision-spend danger. Removed the `collectionSlugs.length > 0` guard in `buildPublicMix()` → the "drops the collection-view write" arm went red (`collection_view` present when it should have been absent). **No RC filed** — the guard was green for the right reason in all four cases; falsification confirmed the test fails for the reason it claims to, not vacuously. |
| Runbook | `docs/SCALING.md (Load Testing section)` — prereqs (k6 v0.50+ install paths + Docker path + `k6 inspect`), the bearer-token recipe (via §7A.3's `REVIEW_PHONE`/`REVIEW_OTP` bypass or a real staging login), the full env matrix (`LOADTEST_BASE_URL`, `LOADTEST_RATE`, `LOADTEST_DURATION`, `LOADTEST_SEARCH`, `LOADTEST_BEARER`), the 200/min-per-IP ceiling explained plainly with both ways past it, how to read `rate_limited` and per-endpoint `http_req_duration{endpoint:…}` tags, safety rails (prod refused, scripts must not run concurrently against one host, nothing uploaded/created), the explicit table of what is deliberately not measured and why (`POST /v1/products`, `retag`/`studio-shoot`/`detect-color`/`pro-cleanup`, `/v1/admin/*`, `/v1/auth/*`, `/v1/public/stylist`, opt-in search), and the "this is not a pen test" statement pointing at `security.test.ts`/`admin.login.test.ts`. |
| Left as an owner decision | The 200/min-per-IP limiter is the honest ceiling on real concurrency from one machine — going beyond it needs either multiple generators on multiple IPs, or making the limiter env-overridable on staging (a change to a security control). Neither was built here; both are named in the runbook as the owner's call. |
| Suites re-run fresh | `apps/api` **1431 passed \| 5 skipped (1436)**, 99 files (the load-test suite's 54 tests are new since §7A.7's 1377/1382 baseline) · `apps/web` **331/331** · `apps/mobile` **107/107**. tsc `--noEmit` clean in `apps/api` (the one outstanding error, `TS2532` on `entries[entries.length - 1].name` at line 369, fixed via `entries.at(-1)?.name` + a truthy assertion). |
| Verdict | **§7A.5 done — §7A is now complete (§7A.1–§7A.7).** Remaining board work is owner-only (§7B) or blocked on an EAS build (§5B `?ref=` capture, F-038 T8 mobile "Refer & Earn"). Separately open: §2.1 (`purge-rls-live.test.ts`, 5 skipped, needs local Docker Postgres). |

## 2026-09-25 (launch §5B.1) — `?ref=` referral capture on `/for-retailers`, scope reduced by a real blocker

**Commit:** *(this task)* · `apps/web/src/app/for-retailers/page.tsx` + `apps/web/src/app/for-retailers/__tests__/page.test.tsx`. No migration, no `apps/mobile` file, no `apps/api` file.

| Piece | Detail |
|---|---|
| What §5B.1 asked for | Board: a `/r/<CODE>` page with a Play Store button + Install Referrer (`&referrer=ref%3D<CODE>`) + a `kanchuki://signup?ref=<CODE>` deep link for installed apps. |
| The blocker, found before writing code | Grepped the whole repo for `play.google.com` — **zero matches anywhere.** `for-retailers/page.tsx`'s own `COMING_SOON` table already says so: *"Play Store / iOS app listings — Coming soon, Android APK available now."* Android's Install Referrer is a Play-Console-only mechanism; there is no listing to attach it to. Building the capture UI for a mechanism that cannot fire yet is the RC-025 shape this codebase repeatedly calls out and fixes (described in one place, implemented in none) — confirmed with the owner via a 3-option question before proceeding, rather than building it unilaterally. |
| Decision | **Build only what works today.** Skip the Play Store button and Install Referrer entirely (ponytail comment marks the upgrade path once §7B.2 ships a real listing); keep the `?ref=<CODE>` capture and the already-installed-app deep link, since both work end to end right now via the code T4 already shipped. |
| Link shape — reused, not duplicated | T3 (2026-09-22) already mints `buildReferralLink()` → `/for-retailers?ref=<CODE>`. The board's `/r/<CODE>` would have been a **second** referral-link shape for the same feature — rejected; `?ref=` capture was added to the existing `/for-retailers` page instead. |
| `apps/web/src/app/for-retailers/page.tsx` | Now takes `searchParams: Promise<{ ref?: string }>`. `REF_CODE_PATTERN = /^KAN-[ABCDEFGHJKMNPQRSTUVWXYZ23456789]{6}$/` — a **cosmetic display gate only** (matches the real affiliate-code alphabet from `apps/api/src/lib/referral-codes.ts`, not imported since it's API-only code and this is a display check, not a security boundary — T4's server-side write is the real validator, same posture the existing `retailers-referral.ts` file documents for why no public code-lookup endpoint exists). A valid-shaped `?ref=` renders a small banner: the code, an explanation ("enter this code when you set up your shop in the app"), and a `kanchuki://onboarding?ref=<CODE>` deep link. Missing or garbage `?ref=` renders the page exactly as before — no banner, no crash. |
| Guard | New `apps/web/src/app/for-retailers/__tests__/page.test.tsx`, **5/5**: valid code renders the banner + correctly-encoded deep link; absent `?ref=` renders nothing; a garbage string renders nothing; the F-018 staff-code shape (`MEERA1`, no hyphen, not `KAN-` prefixed) is rejected; the hyphen-stripped affiliate shape (`KAN7F3QMP`, the RC-031 shape) is rejected. Falsified: widened `REF_CODE_PATTERN` to `/^.*$/` → the 3 negative-arm tests went red for the right reason (banner appeared where it shouldn't), restored. |
| Found while building — RC-041, not fixed | Cross-checked the deep-link package name against `apps/mobile/app.json` before reusing the `intent://`/`kanchuki://` bridge pattern from `apps/web/src/app/join/page.tsx`, and found that page already has the **wrong** Android package (`in.kanchuki.app` vs the real `app.kanchuki.retailer`) — a pre-existing bug, out of scope for this page, not fixed here. The new `for-retailers` deep link uses the correct package-free `kanchuki://` scheme form and does not copy the error forward. Recorded per this repo's "found but deferred → write it down" convention rather than silently carried or silently fixed off-task. |
| What's still not built | **§5B.2–§5B.3** (mobile reads the deep link's `ref` param and prefills the existing manual code field) needs `apps/mobile` changes + an EAS build — explicitly deferred, rides with T8 or not per the owner's call. The `kanchuki://onboarding?ref=<CODE>` link therefore opens the app today but does **not yet prefill anything** — stated, not hidden, via the ponytail comment at the call site. |
| Suites | Web **336/336** (was 331 — +5 new), tsc `--noEmit` clean. |
| Verdict | §5B.1 done in reduced, honest scope. §5B.2/§5B.3 (mobile) and T8 remain EAS-gated / owner-deferred, unchanged from before this session. |

## 2026-09-25 (same day, later) — keyboard insets become shared primitives: `<Sheet>` (web) + `KeyboardScreen` (mobile), and the fix is enforced by derivation instead of by remembering (`RC-042`)

**Commit:** *(this task)* · `apps/web/src/components/Sheet.tsx` + `apps/web/src/components/__tests__/Sheet.test.tsx` + `apps/web/src/lib/{keyboard-inset.ts,__tests__/keyboard-inset.test.ts}` + `apps/web/src/app/admin/__tests__/keyboard-inset.test.ts` (rewritten) + `apps/mobile/src/components/KeyboardScreen.tsx` + `apps/mobile/src/components/KeyboardScreen.test.tsx` + `apps/mobile/__tests__/keyboard-screen-adoption.test.ts` + 36 migrated `apps/mobile/app/**` screens + 11 web surfaces. No migration, no `apps/api` file.

| Piece | Detail |
|---|---|
| Why, after the per-surface fixes already landed | The earlier pass corrected every overlay that existed. That leaves the shape that produced them: `position: fixed` does not reflow for the iOS keyboard, so the inset is a per-instance correction, and a per-instance correction with no owner is a bug on a timer — the next sheet is simply born missing it, and the failure is invisible (valid layout, covered content, no log, no red test). So the fix moved up a level: one component per platform owns it, and the guard **derives** the set of sites that must adopt it rather than listing them. |
| `apps/web/src/components/Sheet.tsx` (new) | The fixed overlay + panel + scrim. `open` is required and the component renders its **own** `AnimatePresence`, so hosts mount it unconditionally — a surrounding `{open && <Sheet/>}` unmounts the sheet before its exit animation (an `AnimatePresence` only animates direct `motion` children, and `<Sheet>` is not one). `onClose` fires on the scrim only: the panel stops propagation. Motion is passed through (`overlayAnimation`/`panelAnimation`, four `MotionProps` each) so every existing `initial/animate/exit` pair is preserved verbatim. `overlayClassName` is **required in practice** by the guard — the base supplies only `fixed inset-0`, and the per-surface scrim colour, centring and z-index (`z-50` vs `z-[70]`) stay per-surface. The inset is applied as `paddingBottom` on the overlay, always, when the keyboard is up: a bottom-anchored panel is lifted clear of the keyboard, and a `max-h-full` panel gives back the same amount by itself because a percentage cap resolves against that same content box. `maxHeightVh` is the **opt-in** for `vh`-based caps (`calc(Nvh - inset)`), which are measured against the *layout* viewport and therefore do **not** respond to the overlay's padding. With no keyboard the style is `undefined`, not `0`, so the closed layout carries no inline style at all. |
| `apps/web/src/lib/keyboard-inset.ts` | The measurement, unchanged in behaviour: `computeKeyboardInset(innerHeight, viewport)` returns `0` when there is no `visualViewport`, when a value is non-finite, when `scale > 1` (zoomed), and clamps negatives; otherwise `round(innerHeight - (viewport.height + offsetTop))`. `subscribeKeyboardInset` attaches four listeners (`visualViewport` `resize`/`scroll`, window `resize`/`orientationchange`), fires once on subscribe, detaches all four on unsubscribe, no-ops under SSR. `useKeyboardInset(enabled)` reads `0` and attaches **nothing** while disabled — which matters because a page component stays mounted whether or not its sheet is open, so an unconditional hook would hold four listeners for the whole session. |
| `Sheet` adoption — 11 web surfaces | The AI Stylist sheet (`app/c/[slug]/components/AIStylist.tsx`) and the customer-web consent modal (`CustomerConsentModal.tsx` — `z-[70]`, bottom-sheet on mobile / centred on desktop, `max-h-[95vh]`), plus **9 admin files / 11 dialog render sites**: `ai-providers`, `commission` (add expense + expense detail — two `<Sheet>`s in one file), `customers` (block), `database/query` (save query), `festivals`, `operations/pending` (reject), `retailers/[id]` (suspend), `team-members` (`MemberModal`, one component rendered for add and delete), `photo-cleanup-test/EffectsCatalog` (preset dialog, `maxHeightVh={92}`). All four previously distinct overlay shapes — centred `motion.div`, plain `div`, `<dialog open>`, and the AI Stylist's `flex-col` + `mt-auto` — collapse onto the one component; nine inline `useKeyboardInset` call sites and their hand-written styles are deleted. |
| The guard, rewritten to assert adoption | `apps/web/src/app/admin/__tests__/keyboard-inset.test.ts` no longer greps for the magic style string; it **derives** the candidate set from the sources — every `.tsx` under `app/admin` containing both `fixed inset-0` and a `<(input\|textarea\|select)` — and requires each file to be either adopted (imports `@/components/Sheet`, renders `<Sheet`, passes `overlayClassName`, and contains **no** `useKeyboardInset` or hand-rolled inset style) or excluded with a >20-character reason **and** a self-checking `marker` string that only an input-free overlay carries. 7 exclusions, each a lightbox or a read-only detail modal: `background-images`, `bug-reports`, `photo-cleanup-test/page`, `post-templates`, `studio-styles`, `suits-designs`, `social-templates` (that last one has no `<input>`/`<textarea>` at all, and its case asserts both `<select>`s appear *before* the overlay opens). A second arm scans **all** of `apps/web/src` and asserts `useKeyboardInset` is read by exactly two files — `components/Sheet.tsx` and `lib/keyboard-inset.ts` — which is what makes reverting any surface to inline arithmetic fail by name. |
| `apps/mobile/src/components/KeyboardScreen.tsx` (new) | The screen-level wrapper. One exported constant, `KEYBOARD_BEHAVIOR = Platform.OS === 'ios' ? 'padding' : 'height'`, resolved **once at module load**, and a pass-through `behavior` prop for a screen that genuinely needs something else. iOS must add the inset itself (it does not resize the layout viewport); Android **does** resize it, so shrinking (`height`) is correct there and `padding` would double-count the keyboard. `className` defaults to `flex-1`; a screen with a background passes its own. |
| Mobile migration — 36 files, 1 exemption | 36 files under `app/` now route through the wrapper: the 24 sites wrapped in the earlier pass, plus the 12 that already carried a hand-written `KeyboardAvoidingView` (the auth/onboarding/settings screens and the pre-existing `customer/[id]`, `product/add`, `settings/staff`, `social/create`, `store-profile`), so the platform ternary and the duplicated `className`/`behavior` props exist in **one** file. `grep` is the proof: `KeyboardAvoidingView` appears in `app/` at exactly one file. The exemption is `app/staff/retailer-onboard.tsx`, which pins Android to `behavior={undefined}` (iOS-only padding) deliberately — a different behaviour, not the shared default — and is listed in the guard **with its reason** plus a staleness check. |
| Mobile guard — `apps/mobile/__tests__/keyboard-screen-adoption.test.ts` (new) | Eight arms, source-derived: the raw `KeyboardAvoidingView` may appear only in the exempted file (and the exemption fails if it goes stale or the file disappears); no screen may re-derive `Platform.OS === 'ios' ? 'padding'` (the old copy-paste shape, even when passed through the wrapper's own prop); every file importing the wrapper renders it and imports it from the correct relative depth (computed from the file's depth under `app/`); every file rendering it imports it; the platform decision exists in exactly one place in the wrapper; and the wrapper keeps a test asserting the Android branch. Scanning is done on comment-stripped source, so a tombstone comment explaining the ban stays legal. |
| Falsifications — web | Deleting the overlay's `paddingBottom` → `Sheet.test.tsx` red; removing the `open` gate (so the hook subscribes while closed) → the zero-listeners arm red; a new `admin/__falsify__/page.tsx` overlay holding an input → the derivation named it; converting an excluded lightbox into a field-holder → its own exclusion arm red; reverting `AIStylist` off `<Sheet>` → the hook-scan arm red. All restored. |
| Falsifications — mobile | F1 collapsing the Android branch to `'padding'` → `expected 'padding' to be 'height'` plus the single-definition-site arm; F2 a raw `KeyboardAvoidingView` added in a temp screen → the derivation arm red naming the file; F3 a screen re-deriving the ternary **through** the wrapper's prop → that arm alone red (F2's raw-view arm stayed green, which is the point of splitting them); F4 an import at the wrong depth → the wrong-path arm red naming the file; F5 rendering the wrapper with its import deleted → the orphan-render arm red. All restored. |
| Suites | `apps/web` **392/392** (49 files; was 331 before this work, +61 including the new `Sheet` and hook suites), `tsc --noEmit` clean, `next lint` clean. `apps/mobile` **123/123** (20 files; was 107, +16), `tsc --noEmit` clean, `eslint` clean. |
| Not verified — and stated, not implied | **Real-device behaviour remains unverified on both platforms.** No desktop browser can exercise the web fix: with no software keyboard `visualViewport.height === window.innerHeight`, so the computed inset is always `0` and the fixed-path branch never runs — in Chromium *and* desktop WebKit, including device emulation, so a green desktop run is a false green. Android is a **different mechanism** (`height`, layout-viewport reflow) and needs its own device; nothing in the iOS pass is evidence for it. Both are procedures in the board's **§7B.9**. The `ai-campaign.tsx` screen keeps its pre-existing `behavior="padding"` through the wrapper's prop (behaviour preserved, not silently "fixed"). |

## 2026-09-25 (RC-041) — one module for every web deep link, pinned to the mobile manifest; the wrong Android package on `/join`, and a guard that could not fail

**Commit:** *(this task)* · `apps/web/src/lib/deep-links.ts` + `apps/web/src/lib/__tests__/deep-links.test.ts` + `apps/web/src/app/{join,for-retailers,social/connect}/page.tsx` + `apps/web/src/app/join/__tests__/page.test.tsx`. No migration, no `apps/mobile`/`apps/api` file.

| Piece | Detail |
|---|---|
| The bug | `apps/web/src/app/join/page.tsx` builds an Android `intent://` URL whose `package=` was hardcoded as `in.kanchuki.app`. The real package is `app.kanchuki.retailer` (`apps/mobile/app.json` → `expo.android.package`). An `intent://` URL that names the wrong package does not open the app: Android finds no matching handler and falls through to default intent resolution, so "Open with Android app" on the staff-invite bridge simply did not open Kanchuki. |
| Why it survived | Two reasons, stacked. (1) The page renders the plain `kanchuki://join?token=…` scheme link immediately above it — that one has no package to get wrong and works, so any casual test hit the working path. (2) The page's own suite asserted the `kanchuki://` link and **never** the `intent://` one: the broken string was in the DOM with zero assertions on it. Both are the same failure shape this repo keeps recording — a thing that looks tested because something adjacent is. |
| Why not fixed when found | It was surfaced while building §5B.1 (2026-09-25, earlier) by cross-checking the package name against `app.json` before reusing the bridge pattern — a different page, a pre-existing bug, and not what that task was for. Recorded as RC-041 "found, not fixed" (row 90 / the RC tracker) exactly so it could not be silently carried or silently patched off-task. Fixed in the same session once picked up. |
| Fix — `apps/web/src/lib/deep-links.ts` (new) | `APP_SCHEME` and `ANDROID_PACKAGE` are now the only place the two literal values exist in `apps/web`, with the two builders built on them: `appDeepLink(host, params)` → `kanchuki://<host>?…`, and `androidIntentUrl(host, params)` → `intent://<host>?…#Intent;scheme=<scheme>;package=<pkg>;end`. Params are percent-encoded (a `code`/`state` from a Meta redirect can carry `&`, `=`, `#`, `/`), and an absent/empty param is omitted rather than emitted as `key=`. |
| All four call sites migrated, not just the broken one | `join/page.tsx` (both the scheme link and the `intent://` one), `for-retailers/page.tsx` (the §5B.1 `?ref=` link), `social/connect/page.tsx` (the OAuth callback, which passes `code` + `state` — the values that actually exercised the encoder). Fixing only the broken page would have left three more hand-written strings, each one keystroke away from the same defect; the guard below could not have been written against four copies. |
| The manifest cannot be imported | `apps/mobile/app.json` is a sibling app in the same pnpm workspace, not a package `apps/web` can `import` from; the value must therefore still be *written* twice. That is the honest limit of this fix, and it is why the test — not the import — is what holds the two together: the constants are asserted **equal to the manifest at test time**, so drift is a red suite rather than a silent production bug. |
| Guard — `apps/web/src/lib/__tests__/deep-links.test.ts` (new, **10/10**) | Behavioural: `APP_SCHEME === expo.scheme` and `ANDROID_PACKAGE === expo.android.package` (read from `apps/mobile/app.json`), `ANDROID_PACKAGE` matches the reverse-DNS package syntax, both builders' exact output, absent params omitted rather than emitted empty, `&`/`=`/`#`/`/` percent-encoded, multi-param joining. Structural: no file under `src/` except the module may contain `intent://`, `kanchuki://`, a `package=` literal, or the old wrong package `in.kanchuki.app`; the package literal appears in exactly one file. |
| The assertion that was missing | `apps/web/src/app/join/__tests__/page.test.tsx` now asserts the **full** `intent://` URI including `package=app.kanchuki.retailer`, not just the `kanchuki://` sibling. This is the arm that would have caught the original bug at the time it was introduced, so it belongs in the same change as the fix. |
| Falsified 2 ways | (1) `ANDROID_PACKAGE` changed by one character (`retailer` → `retailers`): the manifest pin, both builder assertions and the join-page assertion all went red. (2) While restoring, the old bad `intent://…package=in.kanchuki.app…` string was typed back into `join/page.tsx`: the join-page assertion went red — **and both source-scan arms stayed green**, which they must not. |
| The guard defect falsification (2) found | The source arms strip comments before scanning, using `.replace(/\/\/[^\n]*/g, '')` — and `intent://` **contains `//`**, so on any line mentioning the scheme the stripper deleted the exact text the arm searches for. Every structural arm in the file was therefore **vacuously green**: it reported "no hand-written link outside the module" for the same reason it would have reported it with the links present. Fixed by only treating `//` as a comment when it is *not* preceded by `:` (`.replace(/(^\|[^:])\/\/[^\n]*/gm, '$1')`), with the reason written at the call site; after which both arms fired and named `app/join/page.tsx`. |
| Method note | This is the **third** flawed or vacuous guard caught by mutation in two sessions — RC-039's F7 resolved its own path from a literal filename (so it scanned the original while a copy was mutated), and this one ate its own subject. The repeated lesson: a green arm reads identically whether the rule holds or the detector is broken, so *the guard's ability to fail* is the thing to test, not the rule it claims to enforce. |
| Suites | Web **402/402** (50 files; was 392, +10), `tsc --noEmit` clean, `next lint` clean. |

## 2026-09-25 (RC-043) — one app identity across three apps, and one comment-stripper for eleven guards that were each carrying their own (two of them broken)

**Commit:** *(this task)* · `packages/shared/src/constants/app-identity.ts` (+ test), `packages/shared/src/testing/strip-comments.ts` (+ test), `packages/shared/package.json`, `packages/shared/src/constants/index.ts`, `apps/api/src/lib/staff-invite.ts`, `apps/web/src/lib/deep-links.ts` (now a re-export) + 3 web pages + its test, and 9 guard files (`api` ×8, `mobile` ×1). No migration; no `apps/mobile` runtime file.

| Piece | Detail |
|---|---|
| Why this followed RC-041 | RC-041's fix put the scheme and package in one web module and pinned them to `apps/mobile/app.json`. Auditing that fix to answer "does anything else carry this value?" turned up a **third** hand-written copy — `apps/api/src/lib/staff-invite.ts` built `` `kanchuki://join?token=${raw}` `` itself — and the mobile app is the third consumer. So the value was still in four places, one of them new to the list. Patching the web module had been the right first move and an incomplete one. |
| `packages/shared/src/constants/app-identity.ts` (new) | `APP_SCHEME`, `ANDROID_PACKAGE`, `IOS_BUNDLE_ID`, and the two builders `appLink(host, params)` → `kanchuki://…` / `androidIntentUrl(host, params)` → `intent://…#Intent;scheme=…;package=…;end`, with params percent-encoded and absent params omitted rather than emitted as `key=`. `@kanchuki/shared` is importable by all three apps, so this is the first time the value has had one home rather than one home per app. |
| `IOS_BUNDLE_ID` is a separate constant | The two ids happen to be equal today and Apple/Google issue them independently, so a test asserting equality would be inventing a rule neither store has. Each is pinned to its own manifest field instead. |
| API | `buildStaffInviteUrl(raw)` is now `appLink('join', { token: raw })`. Output is byte-identical: `randomBytes(32).toString('base64url')` is `[A-Za-z0-9_-]`, so `encodeURIComponent` is a no-op on it — stated at the call site so the next person does not have to re-derive it, and covered by the existing staff-invite tests. |
| Web | `apps/web/src/lib/deep-links.ts` is now a pure re-export (`export { … } from '@kanchuki/shared'`), keeping its name so the four call sites did not have to move, and holding **no literals at all** — so the guard arm becomes "no file under `apps/web/src` outside a test may contain `intent://`, `kanchuki://`, `package=`, the package id or the RC-041 value", which is a stronger claim than "only this module may". The builders were also renamed from `appDeepLink` to `appLink` so there is one name repo-wide. |
| The manifest copy stays — and why is written down, not implied | *(Superseded the same day — see §2026-09-25 (later still) at the end of this file: this copy was the one left over, and it was then removed.)* `apps/mobile/app.json` keeps `expo.scheme` / `android.package` / `ios.bundleIdentifier`, because Expo resolves the manifest *before* any TypeScript runs: it cannot import the shared module, and the web app cannot import a values module from a sibling app either. The alternative — moving the three fields into `app.config.js`/`.ts`, which *can* import a module — was rejected for this session: it changes how the manifest resolves on the release path (`expo prebuild` → Gradle → the signed `.aab`), and it cannot be verified on a Windows host (no prebuild, no EAS build, and a wrong manifest is an unbuildable app). Recorded as a decision someone can take with a device and a build in front of them, not guessed at and not faked with a config file that looks dynamic but is not verified. |
| `app-identity.test.ts` — the pin, and the pin about the pin | *(Superseded the same day — see §2026-09-25 (later still): both arms described here now assert the inverse.)* **11/11.** The constants are asserted equal to the manifest; `app.config.js` is asserted to **not** set `scheme` / `android` / `ios` (it merges `extra.buildInfo` over `app.json` today, so if it ever set one of these, the manifest pin would be checking a file the build no longer reads — the RC-041 shape one layer up, and invisible because both files look individually correct); it must still spread `...config` in, or the pin is meaningless. Then the builders, the encoding, and the docs pin below. |
| Docs pin — the operator copy | `docs/DEPLOY.md` and `docs/PLAY-STORE-RELEASES.md` are filled in by a human reading them (Meta's dashboard asks for the package; the Play Console asks for it), so a wrong value there is a support ticket rather than a crash, and nothing would ever have caught it. Both are scanned: every reverse-DNS-shaped token on a line mentioning `package`/`bundle` must equal one of the two constants, plus the RC-041 value must appear in neither. One exclusion, with its reason: `com.facebook.katana` (Facebook's own package, named in SDK setup, not ours). Historical records (`docs/root-cause/*`, `docs/BUILD-LOG.md`, `CLAUDE.md`, `docs/tasks/**`) are deliberately **not** scanned — they quote the broken value on purpose, and pinning them would forbid writing down what happened. |
| Searched for marketing copy — there is none to pin | `app.kanchuki.retailer` was grepped repo-wide: the only non-doc hits are the shared constant, the two manifest fields, and test expected-values. `apps/web`'s marketing pages contain no package string at all, and the Play Store listing copy (`docs/references/guides/play-store-listing.md`) has none. So the docs pin covers the whole surface, rather than covering the two files that happened to be easy to find. |
| **RC-043** — the audit that was asked for, and what it found | Every source-scanning guard in the repo was checked for the "cannot fail" shape (a detector that does not actually fire). They carried their own `stripComments`/`code()` at **eleven** call sites — six on the first pass, five more found by the re-grep two rows below. **Two used the naive `.replace(/\/\/[^\n]*/g, '')`** — which deletes the `//` in `intent://`, `https://` and `kanchuki://`, i.e. the exact text the arms search for, leaving them vacuously green — and **two more used `(^\|\s)\/\/`**, which misses a comment that follows code with no space, so `x();// idm-vton` survived the strip and failed the build later with a message that made no sense in context. RC-041's own guard was one of the two naive ones. |
| Why nothing noticed, in one line | A broken stripper only ever *removes* text, and "the scan found nothing" is what a working guard says about a clean tree **and** what a vacuous guard says about a dirty one. The two verdicts are the same string, so no assertion could separate them without a mutation. |
| The decisive falsification | Restore the naive stripper **and** append a hand-written `intent://join?token=f#Intent;scheme=kanchuki;package=z;end` link to `apps/web/src/lib/keyboard-inset.ts`: the web guard reports **10/10 passed**. Fix the stripper and leave that link exactly where it is: the same suite fails with `expected [ 'lib/keyboard-inset.ts' ] to deeply equal []`. The guard's ability to fail was the thing with no test. |
| `packages/shared/src/testing/strip-comments.ts` (new) | One implementation for all six. Exposed as the **`@kanchuki/shared/testing`** subpath rather than from the package root, so it is structurally unable to reach an app bundle (`exports` maps only `./testing`, and no runtime module imports it) — a test helper in a production bundle is the kind of thing this repo would flag later. Two clauses carry the fix: `(^\|[^:])` keeps a `//` that follows a `:` intact (the scheme case), and `$1` returns the seam character, because dropping it would join the two sides of the cut and could invent a token that matches or hide one that should have. |
| A correction the helper's own suite made | The first draft of the documented limit said `label: // note` is **not** stripped. The suite refuted it on the first run: the character before `//` is the space, so it *is* stripped; only a `//` **immediately** after a `:` is exempt. The limit is now stated narrowly and that distinction has its own assertion — a doc that overstates a limitation is how the next person concludes their case is uncovered when it is not. |
| A comment that argued for the bug | `apps/mobile/__tests__/keyboard-screen-adoption.test.ts`'s local helper carried a docblock rationalising the damage: *"a truncated line only ever makes the guard **quieter** for that one line, never wrongly red"* — quieter is the entire failure mode, so the comment described the bug while arguing it was acceptable. Removed with the copy when the helper moved to the shared module. Fourth member of this family the repo has recorded (after RC-039's F7, which scanned the original while a copy was mutated); the comment is what stops the next person from looking. |
| The audit's first inventory was wrong, and the re-grep is in the record instead of a footnote | It said **six** call sites and that the regex then existed in one file. Grepping the *shape* rather than trusting the list — `(^\|[^:])` and `[\s\S]*?\*/` outside `packages/shared/src/testing/` — found **five more** still stripping and scanning in `apps/api`: `referral-accrue`, `referral-qualify`, `referral-payout` (×2, the second inline) and `admin-referral-monitor`. All five used the correct form, so **no detector was broken** — the answer to the question that was asked is that the two broken ones were already the two found. But "one implementation" was not yet true, and the same paragraph had claimed the remaining guards only "read specific named files". Migrating the five makes the claim checkable: the grep now returns **nothing** outside the shared module. The lesson is the bug's own lesson one level up — **an inventory nothing re-derives drifts**, so the completeness claim lives in a command, not in prose. |
| The second pass found that the two passes do not commute | A line comment that itself contains `/*` (real case: a route header reading `// mounted under /v1/public/*;`) makes a block-first pass open a block *inside the comment* and run to the next real `*/`, eating the live code between them — so a scan over the result is quiet for the same reason a broken detector is quiet. Two of the five had already hit this and stripped line-first. That order is now an explicit `'line-first'` argument instead of a convention each file re-derived, and both orders are pinned: block-first eats `const keep = 1;` in a constructed case, line-first keeps it, and the two agree when neither rake is present — which is why the default is the migrated callers' existing order and not a claim that either is *right*. |
| The rake fired for real, on the first run after the migration | `apps/api` resolves `@kanchuki/shared/testing` from the built **`dist/`** (the RC-035 trap: gitignored, so stale). The new `'line-first'` argument was silently ignored by the one-argument build still in `dist`, block-first ran, and `admin-referral-monitor.test.ts` went red because the `import … from '../../jobs/referral-payout.js'` line had been eaten. Rebuilt the package (`pnpm --filter @kanchuki/shared build`) and it passed. Two things to keep: the failure *was* the module's own docblock executing, and a signature change to a `dist`-consumed package needs a rebuild before any result is believed. |
| Falsified 7 ways, each red for the right reason and restored | **F1** `ANDROID_PACKAGE` `retailer` → `retailers`: red in three places (shared manifest pin, web manifest pin, `/join`'s page assertion — the arm whose absence let RC-041 ship). **F2** `scheme: 'kanchuki-falsify'` added to `app.config.js`: the merge-only arm red. **F3** `PLAY-STORE-RELEASES.md` set back to `in.kanchuki.app`: two docs-pin arms red, the message naming file **and line** (`docs/PLAY-STORE-RELEASES.md:216`). **F4/F5** the stripper, both directions with the violation untouched — naive form + a hand-written `intent://` link in `keyboard-inset.ts` → the web guard **10/10 green**; correct form + that same link → red, naming `lib/keyboard-inset.ts`. **F6** dropping the `'line-first'` argument at `admin-referral-monitor` → red, the import line gone from the stripped source (the rake, on demand). **F7** changing the helper's default order → the new block-first arm red (`expected '…const keep = 1; …' not to contain 'const keep = 1;'`). |
| Suites | Shared **62/62** (6 files; was 59, +3 order arms), api **1432 passed / 5 skipped** (1437, 100 files), web **402/402** (50), mobile **123/123** (20); `tsc --noEmit` clean ×4, web `next lint` clean, api `biome check src/` clean (332 files). One pre-existing load flake seen once and not reproduced (`products.test.ts:158`, 5s timeout, passes alone) — recorded rather than re-run until green. No migration; no `apps/mobile` runtime file. |
## 2026-09-25 (later still) — the mobile manifest stops repeating the app identity: the shared constant is now what `expo prebuild` reads

**Commit:** *(this task)* · `apps/mobile/app.config.ts` (new — replaces `app.config.js`, which is deleted in the same commit), `apps/mobile/app.json` (−3 lines), `apps/mobile/__tests__/app-config.test.ts` (new), `apps/mobile/src/lib/build-info.ts` (docblock), `packages/shared/package.json` (+ the `./constants/app-identity` subpath), `packages/shared/src/constants/app-identity.ts` (+ its test), `apps/web/src/lib/__tests__/deep-links.test.ts`, `docs/DEPLOY.md`. No migration; no change to any app runtime module.

| Piece | Detail |
|---|---|
| What this closes | The entry above ended with the one copy it deliberately left: `apps/mobile/app.json` held `expo.scheme` / `android.package` / `ios.bundleIdentifier`, and a test asserted them **equal** to the shared constants. "Written twice, held together by a test" is now "written once" — the last of the original four copies, and the only one where drift is not a broken link but an **unbuildable app**: those three fields are what `expo prebuild` turns into the Gradle application id and the Xcode bundle id. The mobile app stops being a consumer-with-a-copy and becomes a plain consumer. |
| The obvious implementation is the broken one — measured, before any of it was written | A `app.config.ts` that imports `@kanchuki/shared` resolves through `exports` to `dist/`, which is gitignored and **is not built on the release path**: `android-release.yml` runs `pnpm install` → `expo prebuild` → Gradle, and `pnpm install` executes only `packages/db`'s postinstall (measured, not assumed). Probed with the atomic version and `dist` moved aside: `Error reading Expo config … Cannot find module …\@kanchuki\shared\dist\index.js` — the config dies *before* Gradle, which is a build that never starts rather than a link that misbehaves. Registering sucrase inside the config does not rescue it either: Expo transpiles **only the config file itself** (`evalConfig` = sucrase + `require-from-string`), so a `require` written in it goes through Node's ordinary resolver, and an extensionless relative `../shared/src/…` fails the same way. |
| What actually works, and why it is a source import on purpose | New subpath `./constants/app-identity` in `packages/shared` whose `default` condition points at **`src/`** — deliberately, so it *cannot* silently resolve to a stale build — plus `require('sucrase/register/ts')` then `require('@kanchuki/shared/constants/app-identity')` in the config. No `dist`, no build step, no ESM/CJS interop, and the literal still exists in exactly one place. The subpath name was chosen so it also resolves through the `@kanchuki/shared/*` wildcard both apps' tsconfigs already declare: `@kanchuki/shared/identity` would have mapped to a non-existent `src/identity` in the mobile program, i.e. a type error in the one file that now depends on it. One rule comes with it, stated in both the module and the config: this subpath is **build-time only**; app runtime code imports `@kanchuki/shared` like every other consumer. |
| `app.config.js` → `app.config.ts`, one file and not two | Expo prefers `app.config.ts` over `app.config.js` when both exist, so leaving the old file in place would have produced exactly one file read by nobody — the RC-041 shape, one layer down — and its `extra.buildInfo` merge would have silently stopped happening. The merge therefore moved into the same file rather than a second one. `app.json` lost precisely three lines; everything else it owns (plugins, `versionCode` 5, permissions, `blockedPermissions`, `infoPlist`, `supportsTablet`, `experiments`) moved untouched, because the config spreads `android` / `ios` rather than replacing them. |
| Proof the move is inert — the check that mattered | Captured the resolved prebuild config (`expo config --type prebuild --json`) **before** the move with `dist` absent, then the same after: identical once key order is normalised (the spread appends `package` / `bundleIdentifier` last, which is not a behavioural difference and is the only diff there was). Comparing a resolved config rather than the two source files is what makes the claim checkable at all — the whole point of this change is that the *resolver* is what Gradle reads, not the JSON. |
| …and the proof caught a transcription error in the edit itself | The rewrite of `app.json` had turned `android.permission.READ_EXTERNAL_STORAGE` into `EXTERNAL_STORAGE` in `blockedPermissions`. A permission name that is not a valid permission is simply ignored by the manifest merger, so this would have shipped as a silent regression in the *blocked* list — the exact mechanism by which `RECORD_AUDIO` had been shipping for months (row 72) — and nothing local would have failed. `git diff` on the manifest (exactly 3 deletions) and the before/after config comparison both showed it; it was fixed before the falsification pass began. |
| `app-identity.test.ts` — two arms invert, one is added | **11/11.** The arm that asserted the constants *equal* the manifest becomes "the manifest does **not** carry a copy" (`undefined`, not equal — a leftover copy would not be wrong, it would be a second source that looks authoritative and is read by nothing, which is how RC-041's wrong package survived a year); the arm that asserted `app.config.js` does not touch these fields becomes "`app.config.ts` imports them, assigns them to `scheme` / `package` / `bundleIdentifier`, and contains **no** literal of either id and no quoted scheme" — with comments stripped first, so a literal reaching those lines is code and the third copy, in the file that is supposed to consume it. Unchanged: it must still spread `...config`, or `app.json` is bypassed and everything else it holds vanishes from the build. |
| `apps/mobile/__tests__/app-config.test.ts` (new) — the only arm that can see a config that fails to load | **2/2.** It evaluates the **real** config through `@expo/config`'s `getConfig` — the same function `expo config` and `expo prebuild` call — and asserts the resolved `scheme` / `android.package` / `ios.bundleIdentifier` equal the constants, plus that `extra.buildInfo` survived the rename (nothing else asserts it, and losing it would take the "which commit is this build from" footer with it). Reading the two files side by side proves nothing here: the import cannot be type-checked and what matters is what the resolver produces. Falsified three ways that a source-reading arm could not catch — the transpile hook removed, the field dropped from the merge, the literal typed back. |
| Web's pin had to change too, and came out stronger | `apps/web/src/lib/__tests__/deep-links.test.ts` compared the **dist**-resolved constants against `apps/mobile/app.json`. Its subject was never the manifest: it is the stale-`dist` trap (RC-035 — `apps/web` also resolves `@kanchuki/shared` through the built package, so a stale build would serve web an old package name while the app used the new one). With the manifest no longer holding the values it now compares dist against **source** (`@kanchuki/shared/constants/app-identity`), which is the same trap stated more precisely, and the manifest half of the job moved to `app-config.test.ts` where the real resolver is available. Web remains **10/10**. |
| Typecheck caught what the JS config never could | `app.config.ts` is inside the mobile TS program (`**/*.ts`), so the implicit-any parameter failed `tsc`: `error TS7031: Binding element 'config' implicitly has an 'any' type`. Typed with Expo's own `ConfigContext` and the return as `ConfigContext['config']` — so from now on a misspelled field name is a type error instead of a key Expo ignores in silence, which is how a config stops doing something without saying so. The old `app.config.js` was unchecked by definition. |
| Falsified 6 ways, each red for the right reason and restored | **F1** the `sucrase/register/ts` line removed → the config cannot load at all (`SyntaxError: Unexpected token 'export'`), mobile arm red. **F2** `scheme: 'kanchuki'` typed back into the config → shared arm red (`expected '…' to match /scheme:\s*APP_SCHEME/`). **F3** `"scheme": "kanchuki"` re-added to `app.json` → `expected 'kanchuki' to be undefined`. **F4** `bundleIdentifier` dropped from the `ios` spread → `expected undefined to be 'app.kanchuki.retailer'` — the arm F4 exists for, since a merge that forgets a field is exactly what a spread makes easy. **F5** `ANDROID_PACKAGE` changed in the source **without** rebuilding `dist` (RC-035's shape) → web red (`expected 'app.kanchuki.retailer' to be 'app.kanchuki.retailers'`) *and* the mobile arm red: two independent detectors, neither of which can see the other's failure. **F6** the new exclusion, checked rather than trusted — `docs/DEPLOY.md:1067`'s id set to `app.kanchuki.retail` → red naming that exact file and line, so adding a *filename* to the skip-list did not blunt the scan for the application id on the same line. |
| Operator doc updated rather than left as a dead pointer | `docs/DEPLOY.md`'s Meta-dashboard facts table said the package's source is `apps/mobile/app.json` → `android.package`. That field no longer exists, so an operator following it would open a file with nothing to find. It now names the dynamic config and the shared module. This required one line in the docs pin: `app.config.ts` is a **filename** that matches the reverse-DNS shape the pin scans for, so it joined `com.facebook.katana` in the reasoned exclusion set — with the same edit, on a package/bundle line, the package id itself is still compared. |
| Suites | Shared **62/62** (6 files), api **1432 passed / 5 skipped** (1437, 100 files), web **402/402** (50), mobile **125/125** (21; was 123, +2), `tsc --noEmit` clean ×4, web `next lint` clean, api `biome check src/` clean. No migration; no app runtime module touched. |
| What is still not verified here, stated rather than implied | The change was verified at the layer that can be verified on this host: what `@expo/config` resolves, which is the input to `expo prebuild`. It was **not** verified by running `expo prebuild` or producing an `.aab`/`.ipa` — no prebuild and no EAS build in this environment, and CI produces the AABs from `main`. The resolved output being identical before and after is the argument that the signed artifacts are unaffected; the next release build is what confirms it, and the AAB manifests record the package it was built under. |
| Correction to the entry above | Two rows in §2026-09-25 (RC-043) state the manifest copy is kept on purpose and that the config is asserted *not* to touch these fields. Both are superseded by this entry — the same-day decision was reversed by the same-day follow-up, and the rows are annotated rather than rewritten, since the reason the first version was chosen (an unverifiable release-path change) is the reason the *second* one had to be measured before it was believed. |
