# Fashion V-Tone v1.5 Try-On Service Dependencies
# Install: pip install -r requirements.txt

# FastAPI server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
python-multipart>=0.0.12

# HTTP client for downloading images
requests>=2.31.0

# Image processing
Pillow>=10.0.0

# ---- FASHN VTON v1.5 (Apache 2.0) ----
# Installs from GitHub: fashn-AI/fashn-vton-1.5
# Auto-downloads model weights (~2.3 GB) on first run to HF cache
# Includes: onnxruntime-gpu (GPU), DWPose (ONNX), fashn-human-parser
fashn-vton @ git+https://github.com/fashn-AI/fashn-vton-1.5.git

# ---- CPU fallback (uncomment to run without GPU) ----
# pip uninstall onnxruntime-gpu && pip install onnxruntime

# ---- Optional: R2 upload ----
boto3>=1.34.0
botocore>=1.34.0
