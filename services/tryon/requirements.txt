# CatVTON Virtual Try-On Service Dependencies
# Install: pip install -r requirements.txt

# FastAPI server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0

# HTTP client for downloading images
requests>=2.31.0
httpx>=0.27.0

# Image processing
Pillow>=10.0.0
numpy>=1.24.0

# ---- Model Dependencies (GPU required for inference) ----
# PyTorch with CUDA support
# Install separately based on your CUDA version:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0
torchvision>=0.16.0

# Diffusers for CatVTON pipeline
diffusers>=0.27.0
# Exact-pinned: unpinned >=4.36.0 resolved to transformers 5.13.0 at build time,
# a major version bump (Jan 2026) with breaking internal API changes
# (AttributeError: no attribute 'all_tied_weights_keys' during safety_checker
# load). 4.57.6 is the last stable 4.x release and still has the torch>=2.6
# CVE-2025-32434 check CatVTON's torch bump depends on.
transformers==4.57.6
accelerate>=0.25.0
safetensors>=0.4.0

# CatVTON model from HuggingFace
#   model_id: zhengchong/CatVTON
# Uses diffusers pipeline under the hood

# ---- Optional: R2 upload ----
boto3>=1.34.0          # Required for S3-compatible R2 upload
botocore>=1.34.0

# ---- Optional: Mask generation ----
rembg>=0.1.0           # Background removal for person mask (CPU fallback available)
opencv-python>=4.8.0   # Alternative mask generation

# ---- Optional: Monitoring ----
prometheus-client>=0.19.0
